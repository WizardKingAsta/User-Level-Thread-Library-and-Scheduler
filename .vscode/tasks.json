{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make clean; make SCHED=PSJF; cd benchmarks; make clean; make test; cd ..",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
